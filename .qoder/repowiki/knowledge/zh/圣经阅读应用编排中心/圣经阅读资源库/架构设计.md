- 采用静态资源扁平化布局，混合使用 JSON 索引与 SQLite 数据库。
- JSON 文件（如 `2k.json`, `LU.json`）定义书籍、章节与段落的映射关系，作为阅读导航的结构化元数据。
- SQLite 文件（如 `s_.db`, `xy.db`）承载具体的经文文本或注释内容，支持高效检索。
- PNG 图片（如 `EO.png`, `KR.png`）作为视觉辅助资源，直接通过文件名引用。