- **双模发布架构**：以 `output` 目录为共享构建产物边界，同时服务于 Cloudflare Pages (Web PWA) 和 Capacitor (Android APK)。
- **统一构建入口**：通过 `main.py` 驱动 `build_system` 处理 `resources` 中的 SQLite/JSON 数据，生成标准化的静态 Web 资产。
- **跨平台桥接**：利用 `capacitor.config.json` 将 Web 资产封装为原生应用，并通过 GitHub Actions 自动化执行从数据编译到多端部署的全链路流程。