- 采用三阶段流水线架构：数据导出（调用 `export_bible_sql_json.py` 处理 SQLite）、静态资源组装（复制 HTML/CSS/JS 并生成 PWA 清单与 Service Worker）、版本配置注入（生成 `version.json` 及 Base64 编码的远程配置）。
- 核心逻辑集中在 `main.py`，通过 `config.yaml` 驱动路径与行为，依赖 `PyYAML` 进行配置解析。
- 数据层通过专用脚本将关系型数据转换为分层 JSON 结构（全局索引与按书卷分片），并在构建时进行体积压缩优化。